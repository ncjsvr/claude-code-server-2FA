# Claude Code Server - Environment Variables
# Copy this file to .env and fill in your values

# Anthropic API key for Claude CLI (optional - you can also use OAuth login via `claude` command)
# ANTHROPIC_API_KEY=sk-ant-xxxxx

# Session cookie signing secret (optional - auto-generated and persisted to volume if not set)
# SESSION_SECRET=

# Session cookie lifetime in milliseconds (optional - default: 30 days)
# SESSION_MAX_AGE=2592000000

# Display name shown on login page and VS Code banner (optional)
# APP_NAME=Claude Code Server

# Volume mount path (optional - default: /home/clauder)
# CLAUDER_HOME=/home/clauder

# Run as root instead of clauder user (optional - default: clauder)
# RUN_AS_USER=clauder
